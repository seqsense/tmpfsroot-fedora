#!/bin/bash

count=0
while [ ${count} -lt 60 ]
do
  /usr/bin/chronyc sources | grep "\^[c+c-c*]" > /dev/null 2>&1
  if  [ $? -eq 0 ]; then
    echo "NTP Server is active"
    /usr/bin/chronyc sources
    exit 0
  else
    echo "NTP Server is not active"
    ((count++))
    sleep 1
  fi
done
